# Require automatic validation and service-unit confirmation

Field completeness can be checked automatically, but the system cannot certify that recorded follow-up and vaccine facts are true. Each month the application will validate required data and errors automatically, then require an authorized service-unit user to confirm submission; district administrators can still close the cycle by the deadline when units are incomplete or unconfirmed, with those statuses displayed explicitly in the dashboard.
