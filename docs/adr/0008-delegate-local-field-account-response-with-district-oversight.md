# Delegate local field-account response with district oversight

Field-worker account access must be governed centrally while lost phones or compromised PINs require prompt local action to protect child data. A district administrator will establish the initial account registry and view all accounts, while each service unit's authorized confirmer may reset PINs or suspend only the อสม./ผรส. accounts attached to that unit, with each account action written to an audit log.
